{
  "name": "veluxe",
  "version": "1.0.0",
  "description": "Estética automotiva - Feito com VueJS e Python",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{js,ts,jsx,tsx,json,vue}": [
      "npx biome check --write --no-errors-on-unmatched"
    ],
    "backend/**/*.py": [
      "cd backend && source venv/bin/activate && black .",
      "cd backend && source venv/bin/activate && isort .",
      "cd backend && source venv/bin/activate && flake8 ."
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@biomejs/biome": "^2.3.14",
    "@commitlint/cli": "^20.4.1",
    "@commitlint/config-conventional": "^20.4.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7"
  }
}
